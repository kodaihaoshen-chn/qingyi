# 轻译源码说明

## 文件

- `Program.cs`：程序入口和版本信息。
- `MainForm.cs`：主窗口、密钥设置窗口、本地朗读和界面交互。
- `Core.cs`：语言方向判断、百度签名与请求、响应解析、DPAPI 凭据存储。
- `Tests.cs`：不使用真实密钥、不产生真实翻译费用的自动检查。
- `app.manifest`：Windows 10/11 与高 DPI 声明。
- `build.ps1`：调用 Windows 自带的 .NET Framework C# 编译器。

## 构建

在 64 位 Windows PowerShell 中进入本目录：

```powershell
.\build.ps1
```

输出为 `轻译.exe`。

构建并运行测试：

```powershell
.\build.ps1 -IncludeTests
.\轻译-测试.exe
```

程序使用 Windows 自带的 .NET Framework、WinForms、DPAPI 和 `System.Speech`，不随包附带第三方运行库。

## 网络边界

代码只向百度通用文本翻译 API 的 HTTPS 端点发送翻译请求：

`https://fanyi-api.baidu.com/api/trans/vip/translate`

输入内容仅在用户按 Enter 或点击“翻译”后发送。本地朗读不联网。
